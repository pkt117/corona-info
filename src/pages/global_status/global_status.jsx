import React from "react";
import styles from "./global_status.module.css";

const GlobalStatus = (props) => {
  return (
    <section className={styles.global}>
      <h1>imsi</h1>
    </section>
  );
};

export default GlobalStatus;
