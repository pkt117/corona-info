import React from "react";
import styles from "./guideline_info.module.css";

const GuidelineInfo = (props) => {
  return (
    <section className={styles.guideline}>
      <h1>imsi</h1>
    </section>
  );
};

export default GuidelineInfo;
